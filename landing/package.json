{
  "name": "goallll-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing landing page variations for goallll iOS app",
  "scripts": {
    "predev": "mkdir -p public/src/img public/src/shared src/img && cp ../src/img/*.png src/img/ && cp ../src/img/*.png public/src/img/ && cp -r src/shared/* public/src/shared/",
    "dev": "npm run predev && vite",
    "build": "npm run predev && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
